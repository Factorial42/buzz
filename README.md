# buzz

## Usage

To run the project, clone the repo with `git clone https://github.com/Factorial42/buzz.git`, and run `pod install` from the Project directory first.

## Requirements
- iOS 9.3+
- Xcode 8.2
- Swift 3.1
